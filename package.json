{
  "name": "bookstore-fullstack",
  "description": "> Projeto de estudo para desenvolvimento Backend (Express) e Frontend (Next.js) com foco em TDD, Clean Architecture e DevOps (Docker).",
  "scripts": {
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "db:migrate": "docker compose exec api npx prisma migrate dev",
    "db:generate": "docker compose exec api npx prisma generate",
    "db:seed": "docker compose exec api npx prisma db seed",
    "logs:api": "docker compose logs api -f",
    "logs:db": "docker compose logs db -f",
    "logs:web": "docker compose logs web -f",
    "logs": "docker compose logs",
    "test": "docker compose exec api npx vitest"
  },
  "author": "Drummonds",
  "license": "MIT",
  "type": "commonjs"
}
